#ifndef _XTASK_WHEN_H_
#define _XTASK_WHEN_H_

#include <memory>
#include <iterator>
#include <initializer_list>
#include <atomic>
#include "Future.h"

namespace xtask{
    template<class Iterator>
    auto when_all() -> std::iterator_traits{
        
    }
}

#endif //_XTASK_WHEN_H_
